# QandA 2.0 项目审查与优化报告

日期：2026-06-21

## 本次范围

- 用户端、管理端、服务端三端整体审查、优化、构建与本地启动。
- 用户端顶部栏统一优化，移除全局“本设备”字段，压缩桌面与移动端顶部栏高度。
- 公告详情支持 Markdown 渲染，修复列表、嵌套列表、代码、图片与移动弹窗排版问题。
- 新增前端版本更新检测，用户停留在旧页面时可发现新版本；练习页不会强行打断答题。
- 系统版本升级为 `2.0.0`，用户端展示为 `QandA v2.0`。
- 根目录新增 `AGENT.md`，记录后续版本号与发布维护规则。

## 主要修复

- 用户端：
  - 首页、题库、统计、公告、我的页顶部栏视觉统一。
  - 练习页“题目全览”改为“答题卡”。
  - 我的页“关于项目”改为系统介绍，不再描述单个页面。
  - 登录页背景改为 CSS 绘制的主题化交互背景，指针移动会改变视觉焦点。
  - 版本更新检查读取 `version.json`，发现新构建后提示刷新；答题中延后提示。

- 公告：
  - 管理端保存公告时保留原始 Markdown。
  - 用户端公告列表、桌面详情、移动端弹窗均使用统一 Markdown 渲染。
  - 移动弹窗内图片和正文宽度受容器约束，超长内容走内部滚动。

- 管理端：
  - 登录页背景改为 CSS 交互背景。
  - 公告新增/编辑弹窗按“基础信息 / 正文内容 / 发布设置”分区，降低拥挤感。
  - 安全设置密码表单调整为更清晰的策略提示、输入区和操作区。

- 部署与版本：
  - `package.json`、工作区包版本升级到 `2.0.0`。
  - 构建时生成 `user-web/dist/version.json`。
  - Nginx 配置加入 `version.json` 禁缓存规则，静态资源使用长期缓存。

## 浏览器验证

使用内置浏览器实际访问并检查：

- 用户端移动视口 `430x932`
  - `/login`：交互背景正常，无横向溢出。
  - `/`：首页正常，顶部不显示“本设备”。
  - `/library`：题库页正常，移动端顶部栏不破坏布局。
  - `/stats`：统计页顶部栏缩小，清空记录按钮保留。
  - `/announcements`：公告列表、详情和移动弹窗可读，无横向溢出。
  - `/me`：关于项目文案正确，版本显示 `QandA v2.0`。
  - `/practice/...`：按钮显示“答题卡”，旧“题目全览”无残留。

- 管理端
  - `/login`：桌面与移动视口背景正常，指针移动会更新背景焦点。
  - `/dashboard`：本地管理员账号可登录。
  - `/announcements`：新增公告弹窗布局稳定，分区清晰，无横向溢出。
  - `/settings`：密码策略与输入表单布局稳定，无横向溢出。

## 自动化验证

已执行：

```bash
npm run check
```

结果：

- 服务端测试：8 个通过。
- 用户端测试：16 个通过。
- 服务端构建通过。
- 用户端构建通过。
- 管理端构建通过。

## 本地运行状态

三端开发服务已重新启动：

- API：`http://127.0.0.1:3000`
- 用户端：`http://127.0.0.1:5173`
- 管理端：`http://127.0.0.1:5174`

## 后续维护提醒

- 每次准备发布时，按 `AGENT.md` 更新版本号后再构建。
- 构建产生的 `version.json` 是用户端发现新版本的依据。
- 不要缓存 `version.json`；可以长期缓存带 hash 的静态资源。
- 答题中的用户不会被立即打断，新版本提示会延后到离开练习页后出现。
