# 首页题目总数字距修复说明

本包基于 `QandA2-dual-mode-clean-package` 继续调整。

## 修复问题

首页“学习数据”卡片中的“题目总数”大号数字存在字距过紧问题，例如 `577` 的数字视觉上粘连，阅读体验较差。

## 修改范围

仅修改：

```text
user-web/src/styles/home-time.css
```

## 修复方式

新增 HomeView 限定样式：

- 去掉大号 SVG 数字的过度负字距影响；
- 设置轻微正字距；
- 启用 tabular numbers；
- 适当增加数字 SVG 容器宽度；
- 移动端和平板端单独做宽度保护。

## 不影响内容

本次未修改：

- 后端接口；
- 数据库结构；
- Prisma migrations；
- 题库文件；
- 登录注册逻辑；
- 练习页逻辑；
- 管理端逻辑。

服务器部署时不需要数据库迁移。
